[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "data-intelligence-sdk"
version = "0.1.0"
description = "Reusable data intelligence orchestration SDK."
requires-python = ">=3.11"
dependencies = [
    "axiom-model-client>=0.1.0",
    "dotenv>=0.9.9",
    "httpx>=0.28.1",
    "langchain>=1.3.11",
    "langchain-openai>=1.3.3",
    "langgraph>=1.2.7",
    "langsmith>=0.9.5",
    "jsonschema>=4.21",
    "mcp[cli]>=1.28.1",
    "deepagents>=0.6.12",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.uv.sources]
axiom-model-client = { path = "../../../AXIOM/packages/axiom-model-client" }

[tool.setuptools.package-data]
"data_intelligence_sdk.templates.pool" = ["*.json"]
